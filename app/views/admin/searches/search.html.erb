<div class="container">
  <div class="row">
    <div class="col-9 offset-1">
      <h4 style="display:inline">"<%= @word %>"検索結果</h4> 
      <h6 style="display:inline;">(全<%= @records.count %>件)</h6>
        <% if @records.empty? %>
          <div class="text-center" style="margin:30px;">
            <p>該当する商品はありません</p>
          </div>
        <% end %>
    　<%= render 'admin/items/index', items: @records %>
    </div>
    <div class="offset-5 my-2"><%= paginate @page %></div>
  </div> 
</div>